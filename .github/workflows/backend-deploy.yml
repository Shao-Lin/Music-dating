name: 🚀 Deploy Backend (dev)

on:
  push:
    branches: [tpproj-21]
    paths:
      - 'backend/**'

jobs:
  deploy:
    name: Deploy Backend
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 🔍 Проверка репозитория (только для получения .env)
      - name: 🔄 Checkout Code (for .env file only)
        uses: actions/checkout@v4
        with:
          ref: tpproj-21
          fetch-depth: 1

      # 📦 Копирование .env файла
      - name: 📦 Deploy Environment File
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "backend/.github/env-files/backend.server.env"
          target: "/home/deployer/music-dating/backend/server/.env"
          overwrite: true

      # 🧹 Подготовка сервера: установка зависимостей
      - name: 🧹 Prepare Server for Build
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "Очистка директории сборки..."
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S rm -rf /home/deployer/music-dating/backend/server/* || true
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S chown -R deployer:deployer /home/deployer/music-dating/backend/server

            echo "Установка зависимостей..."
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S apt update -y
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S apt install -y git openjdk-21-jdk gradle docker.io docker-compose

      # 🌐 Клонирование репозитория на сервер
      - name: 🌐 Clone Repository on Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/deployer/music-dating/backend/server

            echo "Клонирование репозитория..."
            git init
            git remote add origin git@github.com:<ваш-логин>/<ваш-репозиторий>.git
            git fetch origin tpproj-21
            git reset --hard FETCH_HEAD
            git checkout tpproj-21

      # 🏗️ Сборка проекта на сервере
      - name: 🛠 Build Project on Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/deployer/music-dating/backend/server

            echo "Проверка структуры после клонирования..."
            ls -la

            echo "Установка прав на gradlew..."
            chmod +x gradlew

            echo "Запуск сборки Gradle..."
            ./gradlew build --no-daemon

      # 🐳 Запуск контейнеров
      - name: 🐳 Build & Start Containers
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/deployer/music-dating/backend/server

            echo "Остановка старых контейнеров..."
            docker-compose down --remove-orphans

            echo "Запуск новых контейнеров..."
            docker-compose up -d --build --force-recreate